version: '3.8'

services:
  mock-api:
    image: mockoon/cli:latest
    # Command arguments appended to the entrypoint "mockoon-cli start"
    command: ["--data", "/data/mockoon-data.json", "--port", "3000"]
    ports:
      - "3000:3000"
    volumes:
      - ./mockoon-data.json:/data/mockoon-data.json:ro

  frontend:
    build:
      context: .
    ports:
      - "8080:80"
    depends_on:
      - mock-api
